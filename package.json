{
  "name": "nevoa-saude",
  "version": "0.1.0",
  "private": true,
  "description": "Núcleo local executável da plataforma Névoa Saúde.",
  "scripts": {
    "start": "node --no-warnings server.js",
    "test": "node --no-warnings --test tests/*.test.js",
    "check": "node --check server.js && node --check app.js && node --check database.js"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
